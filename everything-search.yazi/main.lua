return {
    entry = function()
            ya.notify({
                title = "es plugin",
                content = "Not in a git directory\nError: ",
                level = "warn",
                timeout = 5,
            })
}
